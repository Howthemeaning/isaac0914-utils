### 项目规约

这是个人工具库，代码会被别的工程 import。规约分两部分：通用编码规约（从
nebula-server 提炼）和库开发专属规约。

#### 通用编码规约

- 注释简洁讲明代码做什么，不要讲是什么，严禁美化艺术装饰，请使用中文
- log，fmt 请使用英文
- 严禁吞掉 golang err，严禁吞掉命令行执行结果报错，严禁命令行 `|| true`
- 禁止 goroutine 复杂调用，凡创建 goroutine 操作必须做内存泄露分析
- 不要因为传递参数不同而创建新方法，用 condition 或 option 收敛。
  例如查找按 uuid、name 等条件走同一个方法，不要写 findByUUID 和 findByName
- 善用设计模式，但不为单次使用的代码做抽象

#### 库开发专属规约

- **不终止调用方进程。** 禁止 `os.Exit`、`log.Fatal`、`panic`，错误一律 return。
  唯一例外是 `ginx.Recovery` 捕获业务 panic 后转成响应
- **不引入包级可变全局状态。** 状态挂在实例上，一个进程要能起多个实例。
  唯一例外是 `logx`，因为 `slog.SetDefault` 本身就是全局的
- **依赖要吝啬。** 库的依赖会传染给每个使用方。加第三方依赖前先想能不能用标准库
  三十行写完——`multiHandler` 就是为此手写的
- **go 指令版本要向下兼容使用方。** 当前钉在 `go 1.24.0` + gin v1.11.0 对齐
  nebula-server，升级前先确认使用方的工具链
- **公开 API 发布后不改签名。** 需要变更时加新函数，旧的留着
- **每个导出符号都要有中文注释说明做什么**，用途不明显的还要说明为什么这么设计
- **必填参数在入口处一次校验完并报全**，不要让人试一次改一个
- **配置解析失败必须报错**，不能静默降级成零值——配错的时候正是最需要知道的时候

#### 验收标准

改完代码必须跑通，不许只跑一部分：

```
GOTOOLCHAIN=local go build ./... && GOTOOLCHAIN=local go vet ./... && GOTOOLCHAIN=local go test -race ./...
```

新增行为要有测试钉住。测试要断言真实行为，不要只断言"没报错"——优雅退出那条用例
断言的是在途请求真的跑完了，而不是 Shutdown 被调用过。
